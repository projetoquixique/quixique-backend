var controller = require("../controllers/artesao.controller.js");
var cors = require('cors');

module.exports = function(app){
    app.use(cors());
    // app.get("/api/artesao");
    // app.post();
    // app.put();
    // app.delete();
}